spring:
  datasource:
    url: jdbc:mysql://localhost:3306/selenium?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: "!Xlfxndnjs21"


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql:true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
  profiles:
    active: local

logging:
  level:
    org:
      hibernate:
        SQL: debug
